SELECT E.ENAME AS EMPLOYEE,E.EMPNO AS EMP_NO,M.ENAME AS MANAGER,M.EMPNO AS MGR FROM EMP E JOIN EMP M ON E.MGR=M.EMPNO;

SELECT ENAME AS EMPLOYEE,EMPNO AS EMP_NO,MGR FROM EMP WHERE MGR IS NULL OR JOB='MANAGER';

SELECT D.DEPT_NO,D.DEPT_NAME,E.ENAME FROM DEPARTMENT36 D LEFT JOIN EMP E ON D.DEPT_NO=E.DEPTNO;

SELECT EMPNO,ENAME,DEPTNO,SAL FROM EMP WHERE SAL IN (SELECT MIN(SAL) FROM EMP GROUP BY DEPTNO);

SELECT ENAME,DEPTNO,SAL FROM EMP WHERE DEPTNO IN (SELECT DEPTNO FROM EMP WHERE COMM > 0) AND SAL IN (SELECT SAL FROM EMP WHERE COMM>0);

SELECT ENAME,DEPTNO,SAL FROM EMP WHERE DEPTNO IN (SELECT D.DEPT_NO FROM DEPARTMENT36 D,EMP E WHERE E.COMM > 0 AND D.HEAD_OFFICE='PUNE') AND SAL IN (SELECT SAL FROM EMP WHERE COMM>0);

SELECT EMPNO,ENAME,JOB,SAL FROM EMP WHERE JOB=(SELECT JOB FROM EMP WHERE EMPNO=7369) AND SAL>(SELECT SAL FROM EMP WHERE EMPNO=7876);

SELECT E.JOB FROM EMP E JOIN EMP A ON (SELECT  WHERE AVG(SAL)=(SELECT MIN(AVG(SAL)) FROM EMP GROUP BY JOB) GROUP BY JOB;

SELECT E.ENAME,E.JOB,E.SAL*12 AS ASAL,E.DEPTNO,D.DEPT_NAME FROM EMP E JOIN DEPARTMENT36 D ON E.DEPTNO=D.DEPT_NO WHERE E.SAL*12=36000 OR E.JOB='CLERK';

SELECT E.ENAME AS EMPLOYEE,E.EMPNO AS EMP_NO,E.SAL AS EMP_SAL,M.ENAME AS MANAGER,M.SAL AS MGR_SAL FROM EMP E JOIN EMP M ON E.MGR=M.EMPNO WHERE E.SAL<M.SAL;

*****************************************************

SELECT D.DEPT_NAME,D.DEPT_NO,
